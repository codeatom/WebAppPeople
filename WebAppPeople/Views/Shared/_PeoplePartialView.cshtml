@model List<Person>

@if (Model.Count() != 0)
{
    <div class="row">
        <div class="col-12">
            @foreach (var item in Model)
            {
                <partial name="../Shared/_PersonPartialView.cshtml" model="item" />
            }
        </div>
    </div>
}
